// Platform-split call screen: CallRoom.native.tsx (Stream Video) on iOS/Android,
// CallRoom.web.tsx (manual WebRTC) on web. Metro resolves the right one.
export { default } from "@/src/calls/CallRoom";
